---
title: Quickstart
description: Create and render a video in 60 seconds
---

<Steps>

## Scaffold

```bash
npx oanim init my-video
cd my-video
```

This creates a Remotion project with `@oanim/core` — animation presets, transitions, components, and design tokens pre-configured.

## Preview

```bash
npx remotion studio
```

Opens Remotion Studio at `localhost:3000` with live preview and timeline scrubbing.

## Compose

Edit `src/MyComp.tsx`:

```tsx
import { AbsoluteFill, useCurrentFrame, useVideoConfig } from 'remotion';
import { fadeUp, Background, SafeArea, palettes } from '@oanim/core';

const colors = palettes.dark;

export const MyComp: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps } = useVideoConfig();

  return (
    <AbsoluteFill>
      <Background gradient={`linear-gradient(135deg, ${colors.bg}, ${colors.bgAlt})`} />
      <SafeArea style={{ justifyContent: 'center', alignItems: 'center' }}>
        <div style={{
          ...fadeUp({ frame, fps, delay: 0.2 }),
          fontSize: 80,
          fontWeight: 700,
          color: colors.text,
        }}>
          Hello World
        </div>
      </SafeArea>
    </AbsoluteFill>
  );
};
```

## Render

```bash
npx oanim render
```

Outputs `out/MyComp.mp4`. Override with flags:

```bash
npx oanim render --fps 60 --res 3840x2160 --codec h265
```

## Generate assets (optional)

```bash
npx oanim login
npx oanim assets gen-image --prompt "dark abstract gradient, purple and blue" --out public/bg.png
```

</Steps>

## Next steps

<CardGroup cols={2}>
  <Card title="Animation Presets" href="/core/element-animations">
    8 animations, 7 springs, 5 easings.
  </Card>
  <Card title="Transitions" href="/core/transitions">
    14 drop-in transition presets.
  </Card>
  <Card title="Composition Patterns" href="/guides/composition-patterns">
    Multi-scene architecture and layering.
  </Card>
  <Card title="Examples" href="/examples">
    6 working example projects.
  </Card>
</CardGroup>

---
allowed-tools:
  - Read
  - "Bash(npx:*)"
---

# /render — Render the current project

Render the current open-animate / Remotion project to MP4.

## Instructions

1. Read `animate.json` in the current directory to understand the project config.
2. Run `npx oanim render` (or `npx remotion render` if no oanim CLI is available).
3. Report the output file path when complete.
